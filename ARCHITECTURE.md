# 🏗️ Architecture - Wallet & Admin System

## 📊 System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        FRONTEND (React)                      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   Home.jsx   │  │  Wallet.jsx  │  │AdminPanel.jsx│     │
│  │              │  │              │  │              │     │
│  │ - Game List  │  │ - Top Up     │  │ - Dashboard  │     │
│  │ - Balance    │  │ - Withdraw   │  │ - Approve/   │     │
│  │ - Nav Buttons│  │ - History    │  │   Reject     │     │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘     │
│         │                 │                  │              │
│         └─────────────────┴──────────────────┘              │
│                           │                                 │
│                    ┌──────▼──────┐                         │
│                    │   api.js    │                         │
│                    │  (Axios)    │                         │
│                    └──────┬──────┘                         │
└───────────────────────────┼──────────────────────────────┘
                            │ HTTP/JSON + JWT
                            │
┌───────────────────────────▼──────────────────────────────┐
│                    BACKEND (Go/Gin)                       │
├───────────────────────────────────────────────────────────┤
│                                                            │
│  ┌────────────────────────────────────────────────────┐  │
│  │              Routes (routes.go)                    │  │
│  │                                                     │  │
│  │  /api/wallet/*     →  WalletHandler               │  │
│  │  /api/admin/*      →  AdminController             │  │
│  └────────────────────────────────────────────────────┘  │
│                           │                               │
│  ┌────────────────────────▼───────────────────────────┐  │
│  │           Middleware Layer                         │  │
│  │                                                     │  │
│  │  AuthMiddleware()  →  Validate JWT Token          │  │
│  │  AdminMiddleware() →  Check User Role = 'admin'   │  │
│  └────────────────────────────────────────────────────┘  │
│                           │                               │
│  ┌────────────────────────▼───────────────────────────┐  │
│  │              Controllers/Handlers                  │  │
│  │                                                     │  │
│  │  WalletHandler:                                    │  │
│  │  - RequestTopUp()                                  │  │
│  │  - RequestWithdraw()                               │  │
│  │  - GetHistory()                                    │  │
│  │                                                     │  │
│  │  AdminController:                                  │  │
│  │  - GetAllTransactions()                            │  │
│  │  - ProcessTransaction()                            │  │
│  │  - GetDashboardStats()                             │  │
│  └────────────────────────────────────────────────────┘  │
│                           │                               │
│  ┌────────────────────────▼───────────────────────────┐  │
│  │              Models (GORM)                         │  │
│  │                                                     │  │
│  │  User:                                             │  │
│  │  - ID, Username, Password, Balance, Role          │  │
│  │                                                     │  │
│  │  Transaction:                                      │  │
│  │  - ID, UserID, Type, Amount, Status               │  │
│  │  - BankName, BankAccount, AccountName             │  │
│  └────────────────────────────────────────────────────┘  │
│                           │                               │
└───────────────────────────┼───────────────────────────────┘
                            │
                    ┌───────▼────────┐
                    │   SQLite DB    │
                    │   (test.db)    │
                    │                │
                    │ - users        │
                    │ - transactions │
                    └────────────────┘
```

---

## 🔄 Data Flow Diagrams

### Top Up Flow

```
┌─────────┐                                    ┌─────────┐
│  USER   │                                    │  ADMIN  │
└────┬────┘                                    └────┬────┘
     │                                              │
     │ 1. Submit Top Up Request                    │
     │    (Amount, Bank Info)                      │
     ├──────────────────────────────►              │
     │                               │              │
     │                          ┌────▼─────┐       │
     │                          │ Database │       │
     │                          │ Status:  │       │
     │                          │ PENDING  │       │
     │                          └────┬─────┘       │
     │                               │              │
     │                               │ 2. View      │
     │                               │    Pending   │
     │                               ◄──────────────┤
     │                               │              │
     │                               │ 3. Approve   │
     │                               ◄──────────────┤
     │                               │              │
     │                          ┌────▼─────┐       │
     │                          │ Database │       │
     │                          │ Status:  │       │
     │                          │ APPROVED │       │
     │                          │ Balance+ │       │
     │                          └────┬─────┘       │
     │                               │              │
     │ 4. Balance Updated            │              │
     ◄───────────────────────────────┤              │
     │                                              │
```

### Withdraw Flow

```
┌─────────┐                                    ┌─────────┐
│  USER   │                                    │  ADMIN  │
└────┬────┘                                    └────┬────┘
     │                                              │
     │ 1. Submit Withdraw Request                  │
     │    (Amount, Bank Info)                      │
     ├──────────────────────────────►              │
     │                               │              │
     │                          ┌────▼─────┐       │
     │                          │ Database │       │
     │                          │ Balance- │       │
     │                          │ Status:  │       │
     │                          │ PENDING  │       │
     │                          └────┬─────┘       │
     │                               │              │
     │ 2. Balance Deducted           │              │
     ◄───────────────────────────────┤              │
     │                               │              │
     │                               │ 3. View      │
     │                               │    Pending   │
     │                               ◄──────────────┤
     │                               │              │
     │                               │ 4a. Approve  │
     │                               ◄──────────────┤
     │                               │              │
     │                          ┌────▼─────┐       │
     │                          │ Database │       │
     │                          │ Status:  │       │
     │                          │ APPROVED │       │
     │                          │ (Done)   │       │
     │                          └──────────┘       │
     │                                              │
     │                          OR                  │
     │                                              │
     │                               │ 4b. Reject   │
     │                               ◄──────────────┤
     │                               │              │
     │                          ┌────▼─────┐       │
     │                          │ Database │       │
     │                          │ Balance+ │       │
     │                          │ Status:  │       │
     │                          │ REJECTED │       │
     │                          └────┬─────┘       │
     │                               │              │
     │ 5. Balance Refunded           │              │
     ◄───────────────────────────────┤              │
     │                                              │
```

---

## 🔐 Security Architecture

```
┌──────────────────────────────────────────────────────┐
│                   Request Flow                        │
└──────────────────────────────────────────────────────┘

Client Request
     │
     ▼
┌─────────────────┐
│ Authorization   │  ← JWT Token in Header
│ Header Check    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ AuthMiddleware  │  ← Validate Token
│                 │  ← Extract UserID
└────────┬────────┘
         │
         ▼
    Is Admin
    Endpoint?
         │
    ┌────┴────┐
    │ YES     │ NO
    ▼         ▼
┌─────────┐  ┌──────────┐
│ Admin   │  │ Process  │
│ Middle  │  │ Request  │
│ ware    │  └──────────┘
└────┬────┘
     │
     ▼
Check Role
= 'admin'?
     │
┌────┴────┐
│ YES     │ NO
▼         ▼
┌────────┐ ┌────────┐
│Process │ │ 403    │
│Request │ │Forbidden│
└────────┘ └────────┘
```

---

## 💾 Database Schema

```
┌─────────────────────────────────────────────────────┐
│                    users                             │
├─────────────────────────────────────────────────────┤
│ id          INTEGER PRIMARY KEY                     │
│ username    TEXT UNIQUE NOT NULL                    │
│ password    TEXT NOT NULL (hashed)                  │
│ balance     INTEGER NOT NULL                        │
│ role        TEXT NOT NULL ('user' or 'admin')       │
│ created_at  DATETIME                                │
│ updated_at  DATETIME                                │
└─────────────────────────────────────────────────────┘
                        │
                        │ 1:N
                        │
┌─────────────────────────────────────────────────────┐
│                 transactions                         │
├─────────────────────────────────────────────────────┤
│ id            INTEGER PRIMARY KEY                   │
│ user_id       INTEGER (FK → users.id)               │
│ type          TEXT ('deposit' or 'withdraw')        │
│ amount        REAL                                  │
│ status        TEXT ('pending','approved','rejected')│
│ bank_name     TEXT                                  │
│ bank_account  TEXT                                  │
│ account_name  TEXT                                  │
│ created_at    DATETIME                              │
│ updated_at    DATETIME                              │
└─────────────────────────────────────────────────────┘
```

---

## 🎯 Component Hierarchy

### Frontend Components

```
App.jsx
├── AuthProvider
│   └── Router
│       ├── Login.jsx
│       ├── Register.jsx
│       ├── Home.jsx
│       │   ├── Navbar
│       │   │   ├── Balance Display
│       │   │   ├── Wallet Button
│       │   │   ├── Admin Button (conditional)
│       │   │   └── Logout Button
│       │   ├── Game Cards
│       │   └── Stats Cards
│       │
│       ├── Wallet.jsx
│       │   ├── Balance Header
│       │   ├── Tab Switcher
│       │   │   ├── Top Up Form
│       │   │   └── Withdraw Form
│       │   └── Transaction History
│       │       └── Transaction Card (map)
│       │
│       └── AdminPanel.jsx
│           ├── Dashboard Stats
│           │   ├── Total Users Card
│           │   ├── Pending Deposits Card
│           │   ├── Pending Withdraws Card
│           │   ├── Total Deposits Card
│           │   └── Total Withdraws Card
│           ├── Filter Tabs
│           └── Transaction Table
│               └── Transaction Row (map)
│                   ├── Transaction Details
│                   └── Action Buttons
```

---

## 🔄 State Management

### Wallet Page State

```javascript
{
  activeTab: 'topup' | 'withdraw',
  balance: number,
  history: Transaction[],
  loading: boolean,
  topupForm: {
    amount: string,
    bank_name: string,
    bank_account: string,
    account_name: string
  },
  withdrawForm: {
    amount: string,
    bank_name: string,
    bank_account: string,
    account_name: string
  }
}
```

### Admin Panel State

```javascript
{
  stats: {
    total_users: number,
    pending_deposits: number,
    pending_withdraws: number,
    total_deposits: number,
    total_withdraws: number
  },
  transactions: Transaction[],
  filter: 'pending' | 'approved' | 'rejected' | '',
  loading: boolean
}
```

---

## 📡 API Endpoints Map

```
┌─────────────────────────────────────────────────────┐
│                  Public Endpoints                    │
├─────────────────────────────────────────────────────┤
│ POST   /register                                    │
│ POST   /login                                       │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│            Protected Endpoints (User)                │
├─────────────────────────────────────────────────────┤
│ GET    /user/me                                     │
│ POST   /user/play-slot                              │
│ GET    /user/history                                │
│                                                      │
│ POST   /api/wallet/topup                            │
│ POST   /api/wallet/withdraw                         │
│ GET    /api/wallet/history                          │
│                                                      │
│ POST   /api/mythic/spin                             │
│ GET    /api/mythic/history                          │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│         Protected Endpoints (Admin Only)             │
├─────────────────────────────────────────────────────┤
│ GET    /api/admin/transactions                      │
│ POST   /api/admin/transactions/:id/process          │
│ GET    /api/admin/dashboard                         │
└─────────────────────────────────────────────────────┘
```

---

## 🎨 UI/UX Flow

### User Journey - Top Up

```
1. Login → Home Page
           ↓
2. Click "💳 Wallet" Button
           ↓
3. Wallet Page Loads
   - See current balance
   - See transaction history
           ↓
4. Click "Top Up" Tab
           ↓
5. Fill Form:
   - Amount
   - Bank Name
   - Account Number
   - Account Name
           ↓
6. Click "Submit Top Up"
           ↓
7. Success Message
   "Top up request submitted!"
           ↓
8. See new transaction in history
   Status: PENDING (yellow)
           ↓
9. Wait for admin approval
           ↓
10. Refresh → Status: APPROVED (green)
    Balance increased!
```

### Admin Journey - Approve Transaction

```
1. Login as Admin → Home Page
           ↓
2. Click "👑 Admin" Button
           ↓
3. Admin Panel Loads
   - See dashboard stats
   - See pending count
           ↓
4. Click "Pending" Tab
           ↓
5. See list of pending transactions
   - User ID
   - Type (deposit/withdraw)
   - Amount
   - Bank details
           ↓
6. Review transaction details
           ↓
7. Click "Approve" or "Reject"
           ↓
8. Confirm action
           ↓
9. Success Message
   "Transaction processed successfully!"
           ↓
10. Transaction removed from pending
    Stats updated
    User balance updated (if deposit approved)
```

---

## 🔧 Technology Stack

```
┌─────────────────────────────────────────┐
│            Frontend Stack                │
├─────────────────────────────────────────┤
│ React 18                                │
│ React Router DOM                        │
│ Axios                                   │
│ Tailwind CSS                            │
│ Framer Motion                           │
│ Vite                                    │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│            Backend Stack                 │
├─────────────────────────────────────────┤
│ Go 1.21+                                │
│ Gin Web Framework                       │
│ GORM (ORM)                              │
│ SQLite (Database)                       │
│ JWT (Authentication)                    │
│ bcrypt (Password Hashing)               │
└─────────────────────────────────────────┘
```

---

## 📊 Performance Considerations

### Database Queries
- Use indexes on `user_id` and `status` for faster filtering
- Use database transactions for atomic operations
- Implement pagination for large transaction lists

### Frontend Optimization
- Lazy load admin panel (code splitting)
- Debounce form inputs
- Cache dashboard stats
- Implement virtual scrolling for long lists

### Security Best Practices
- JWT token expiration
- Rate limiting on API endpoints
- Input sanitization
- SQL injection prevention (GORM handles this)
- XSS prevention (React handles this)

---

## 🚀 Scalability

### Current Architecture
- Single server
- SQLite database
- Suitable for: Small to medium applications

### Future Scaling Options
1. **Database**: Migrate to PostgreSQL/MySQL
2. **Caching**: Add Redis for session/stats caching
3. **Load Balancing**: Multiple backend instances
4. **CDN**: Serve static frontend assets
5. **Microservices**: Split wallet/admin into separate services
6. **Message Queue**: Add RabbitMQ for async processing

---

**Architecture Documentation Complete! 🏗️**
